#ifndef FILEMANAGER
#define FILEMANAGER
#include <iostream>
#include <fstream>
#include <sstream>

class FileManager{
    private:

    public:
    void newCSV(int array[], int n, std::string isSorted);
};

#endif